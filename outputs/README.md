# Outputs

Generated artifacts are separated by type: `checkpoints/`, `figures/`, `heatmaps/`, `results/`, and `logs_tensorboard/`. All are excluded from Git by default because they can be large, machine-specific, or derived from restricted data. Preserve a manifest with the config, dataset version, and command when a result is used in research.
