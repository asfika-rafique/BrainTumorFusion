# Legacy code

These files were preserved because they were part of the original project but do not form a verified active path. In particular, the text-fusion path contains a placeholder encoder, the old inference pipeline imports missing symbols, and the original `.gitignore` was actually a legacy prediction helper.
