# User interfaces

`streamlit_app.py` is the active image-only research demonstration and uses the same reusable inference and Grad-CAM code as the command-line tools. `app_desktop.py` is a small Tkinter viewer. Both require a compatible checkpoint and are for research inspection only, not clinical use. Text fusion is not implemented.
